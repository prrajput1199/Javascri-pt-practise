//whenever js engine tries to execute js code a global execution context is created and post inside the callstack


//All the callback functions which comes through promises and mutation observer goes into microtask queue

// priority microtask queue >> callback queue

